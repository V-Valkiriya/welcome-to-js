// #todo

'use strict';

// declare and init: first = angus;
let first = 'angus';
// declare and init: last = croll;
let last = 'croll';

// read: first = angus;
// read: last = croll;
// declare and init: greeting = hello angus croll;
let greeting = 'hello ' + first + ' ' + last;

// read: greeting = 'hello angus croll';
// assign: 'hello angus croll!'
greeting = greeting + '!';

console.log(greeting); // 'hello angus croll!'
