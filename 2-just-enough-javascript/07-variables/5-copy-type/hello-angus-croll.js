// #todo

'use strict';

// declare and init: first = 'angus';
let first = 'angus';
// declare and init: last = 'croll';
let last = 'croll';

// read: first = 'angus';
// read: last = 'croll';

// declare and init: greeting = 'hello ' + first + ' ' + last;
let greeting = 'hello ' + first + ' ' + last;

// read: greeting = 'hello angus croll';
// assign: greeting = greeting + '!';
greeting = greeting + '!';

console.log(greeting); // 'hello angus croll!'
