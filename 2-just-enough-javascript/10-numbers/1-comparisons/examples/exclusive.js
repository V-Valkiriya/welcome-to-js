// #todo

'use strict';

// let number = -1;
// let number = 0;
// let number = 0.5;
// let number = 1;
let number = 1.5;
// let number = 2;
// let number = 2.5;
// let number = 3;

console.log(number + ' is between 1 and 2 (excluding 1 and 2)'); // '1.5 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number <= 1); // false

// number is greater than 1 AND less than 2
console.log(number > 1 && number < 2); // true

// number is greater than 2
console.log(number >= 2); // false

let number2 = -1;

console.log(number2 + ' is between 1 and 2 (excluding 1 and 2)'); // '-1 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number2 <= 1); // true

// number is greater than 1 AND less than 2
console.log(number2 > 1 && number2 < 2); // false

// number is greater than 2
console.log(number2 >= 2); // false

let number3 = 0;

console.log(number3 + ' is between 1 and 2 (excluding 1 and 2)'); // '0 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number3 <= 1); // true

// number is greater than 1 AND less than 2
console.log(number3 > 1 && number3 < 2); // false

// number is greater than 2
console.log(number3 >= 2); // false

let number4 = 0.5;

console.log(number4 + ' is between 1 and 2 (excluding 1 and 2)'); // '0.5 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number4 <= 1); // true

// number is greater than 1 AND less than 2
console.log(number4 > 1 && number4 < 2); // false

// number is greater than 2
console.log(number4 >= 2); // false

let number5 = 1;

console.log(number5 + ' is between 1 and 2 (excluding 1 and 2)'); // '1 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number5 <= 1); // true

// number is greater than 1 AND less than 2
console.log(number5 > 1 && number5 < 2); // false

// number is greater than 2
console.log(number5 >= 2); // false

let number6 = 2;

console.log(number6 + ' is between 1 and 2 (excluding 1 and 2)'); // '2 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number6 <= 1); // false

// number is greater than 1 AND less than 2
console.log(number6 > 1 && number6 < 2); // false

// number is greater than 2
console.log(number6 >= 2); // true

let number7 = 2.5;

console.log(number7 + ' is between 1 and 2 (excluding 1 and 2)'); // '2.5 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number7 <= 1); // false

// number is greater than 1 AND less than 2
console.log(number7 > 1 && number7 < 2); // false

// number is greater than 2
console.log(number7 >= 2); // true

let number8 = 3;

console.log(number8 + ' is between 1 and 2 (excluding 1 and 2)'); // '3 between 1 and 2 (excluding 1 and 2)'

// number is less than or equal to 1
console.log(number8 <= 1); // false

// number is greater than 1 AND less than 2
console.log(number8 > 1 && number8 < 2); // false

// number is greater than 2
console.log(number8 >= 2); // true
